

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>

      <%= f.label :image %>
      <%= f.text_field :image, class: 'form-control' %>
      <%#= f.file_field  :image, multiple: false, accept:'image/png', class: 'form-control' %>

      <%= f.submit "Edit user's image", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>